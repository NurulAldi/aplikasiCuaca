<!DOCTYPE html>
<html lang="en">
{{>head}}
<style>
    *, *::before, *::after { box-sizing: border-box; }
    html, body { width: 100%; height: 100%; margin: 0; padding: 0; }

    body.nf-page {
        display: block;
        max-width: none;
        margin: 0; padding: 0;
        background: #141414;
        color: #e5e5e5;
        font-family: "Segoe UI", Roboto, Arial, sans-serif;
        overflow-x: hidden;
        min-height: 100vh;
    }

    .nf-viewport {
        width: 100%;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
    }

    .nf-page header { margin: 0; padding: clamp(12px, 2vmin, 20px) 4vw; }
    .nf-page header a { color: #b3b3b3; text-decoration: none; margin-right: 16px; }
    .nf-page header a:hover { color: #ffffff; }
    .nf-page footer { margin: 0; padding: clamp(12px, 2vmin, 16px) 4vw; color: #8c8c8c; border-top: 1px solid rgba(255,255,255,0.12); }

    .nf-main {
        position: relative;
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: clamp(16px, 4vmin, 40px) 4vw;
        padding-bottom: 60px;
    }

    .nf-main::before {
        content: "";
        position: absolute; inset: 0;
        pointer-events: none;
        background: radial-gradient(120% 120% at 10% 10%, rgba(229,9,20,.08), transparent 60%),
                                linear-gradient(180deg, rgba(0,0,0,.35), transparent 40%, rgba(0,0,0,.45));
    }

    .page-header {
        width: min(1200px, 92vw);
        margin-bottom: 40px;
        position: relative;
        z-index: 1;
    }

    .page-title {
        margin: 0;
        font-weight: 800;
        font-size: clamp(32px, 6vw, 56px);
        color: #ffffff;
        letter-spacing: .5px;
        text-shadow: 0 2px 12px rgba(0,0,0,.6);
    }

    .nf-accent { color: #E50914; }

    .news-grid {
        width: min(1200px, 92vw);
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 24px;
        position: relative;
        z-index: 1;
    }

    .news-card {
        background: #1f1f1f;
        border-radius: 8px;
        overflow: hidden;
        border: 1px solid rgba(255,255,255,0.1);
        transition: all 0.3s ease;
        display: flex;
        flex-direction: column;
    }

    .news-card:hover {
        border-color: rgba(229, 9, 20, 0.6);
        transform: translateY(-8px);
        box-shadow: 0 12px 32px rgba(0,0,0,.5);
    }

    .news-image {
        width: 100%;
        height: 200px;
        object-fit: cover;
        background: linear-gradient(135deg, #2a2a2a 0%, #1a1a1a 100%);
    }

    .news-placeholder {
        width: 100%;
        height: 200px;
        background: linear-gradient(135deg, #2a2a2a 0%, #1a1a1a 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        color: #666;
        font-size: 14px;
    }

    .news-content {
        padding: 20px;
        flex: 1;
        display: flex;
        flex-direction: column;
    }

    .news-meta {
        display: flex;
        gap: 12px;
        margin-bottom: 12px;
        flex-wrap: wrap;
    }

    .news-source {
        font-size: 12px;
        color: #E50914;
        font-weight: 600;
        text-transform: uppercase;
    }

    .news-date {
        font-size: 12px;
        color: #8c8c8c;
    }

    .news-title {
        font-size: 18px;
        font-weight: 700;
        color: #ffffff;
        margin: 0 0 12px 0;
        line-height: 1.3;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    .news-description {
        font-size: 14px;
        color: #b3b3b3;
        line-height: 1.6;
        margin: 0 0 16px 0;
        flex: 1;
        display: -webkit-box;
        -webkit-line-clamp: 3;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    .news-author {
        font-size: 13px;
        color: #8c8c8c;
        font-style: italic;
    }

    .news-link {
        display: inline-block;
        margin-top: 12px;
        padding: 8px 16px;
        background: rgba(229, 9, 20, 0.2);
        color: #E50914;
        text-decoration: none;
        border-radius: 4px;
        font-size: 14px;
        font-weight: 600;
        transition: all 0.3s ease;
        border: 1px solid rgba(229, 9, 20, 0.3);
    }

    .news-link:hover {
        background: #E50914;
        color: #ffffff;
        border-color: #E50914;
    }

    .loading {
        text-align: center;
        padding: 60px 20px;
        color: #b3b3b3;
        font-size: 18px;
    }

    .error {
        text-align: center;
        padding: 60px 20px;
        color: #E50914;
        font-size: 18px;
    }
</style>
<body class="nf-page">
    <div class="nf-viewport">
        <header>
            <a href="/">Home</a>
            <a href="/tentang">Tentang</a>
            <a href="/bantuan">Bantuan</a>
            <a href="/berita">Berita</a>
        </header>
        <main class="nf-main">
            <div class="page-header">
                <h1 class="page-title">Berita <span class="nf-accent">Terkini</span></h1>
            </div>

            {{#if error}}
                <div class="error">{{error}}</div>
            {{else if news}}
                <div class="news-grid">
                    {{#each news}}
                        <article class="news-card">
                            {{#if image}}
                                <img src="{{image}}" alt="{{title}}" class="news-image" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                                <div class="news-placeholder" style="display:none;">No Image</div>
                            {{else}}
                                <div class="news-placeholder">No Image</div>
                            {{/if}}
                            <div class="news-content">
                                <div class="news-meta">
                                    <span class="news-source">{{source}}</span>
                                    <span class="news-date">{{formatDate published_at}}</span>
                                </div>
                                <h2 class="news-title">{{title}}</h2>
                                <p class="news-description">{{description}}</p>
                                {{#if author}}
                                    {{#unless (eq author "Unknown")}}
                                        <p class="news-author">By {{author}}</p>
                                    {{/unless}}
                                {{/if}}
                                <a href="{{url}}" target="_blank" rel="noopener noreferrer" class="news-link">Baca Selengkapnya →</a>
                            </div>
                        </article>
                    {{/each}}
                </div>
            {{else}}
                <div class="loading">Memuat berita...</div>
            {{/if}}
        </main>
        {{>footer}}
    </div>
</body>
</html>
